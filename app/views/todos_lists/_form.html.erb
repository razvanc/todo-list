<%= form_for(@todos_list) do |f| %>
  <% if @todos_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@todos_list.errors.count, "error") %> prohibited this todos_list from being saved:</h2>

      <ul>
      <% @todos_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
